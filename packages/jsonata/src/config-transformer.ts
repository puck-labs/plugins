/**
 * Placeholder for config transformer
 * This will transform Puck config to add static/dynamic mode switcher
 */

import type { Config } from "@measured/puck";

export function withExpressions<T extends Config>(config: T): T {
  // TODO: Implement config transformation
  // This is a placeholder for the MVP setup
  return config;
}
